{% extends 'base.html.twig' %} {% block title %}Register{% endblock %} {% block
body %}
<h1>Register</h1>
<section class="tickes">
  <div class="card trickcard_style">
    {{ form_start(registrationForm) }}

    <div class="form_row">
      {{ form_row(registrationForm.firstName,{'attr':{'placeholder':"Prénom"}})
      }}
    </div>
    <div class="form_row">
      {{ form_row(registrationForm.second_name,{'attr':{'placeholder':"Nom"}})
      }}
    </div>
    <div class="form_row">
      {{ form_row(registrationForm.email,{'attr':{'placeholder':"Address email"






      }}) }}
    </div>

    <div class="form_row">
      {{ form_row(registrationForm.password.first) }}
    </div>
    <div class="form_row">
      {{ form_row(registrationForm.password.second) }}
    </div>

    {# {{ form_row(registrationForm.email) }}
    {{
      form_row(registrationForm.plainPassword, {
        label: "Password"
      })
    }}
    #} {# {{ form_row(registrationForm.agreeTerms) }} #}

    <button type="submit" class="btn btn-lg btn-primary">Register</button>
    {{ form_end(registrationForm) }}
  </div>
</section>
{% endblock %}
